# Restaurant Investment

Project workspace for calculating stakeholder ownership share in a restaurant setup.

## Structure

- `app/` — web app files
- `docs/` — project notes and formulas
- `data/` — sample input data
- `tests/` — test cases

## Quick start

Open `app/index.html` in browser.
