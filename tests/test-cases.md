# Test Cases (MVP)

1. Normal case with both monetary and non-monetary values.
2. Sum monetary = 0, sum non-monetary > 0.
3. Sum non-monetary = 0, sum monetary > 0.
4. Both sums = 0 (should show validation error).
5. Rounding totals must display exactly 100.00%.
